-- Web Development LSP Configuration (HTML, CSS, JS, React)
return {
  name = "react",

  dependencies = {},

  mason_tools = {},

  servers = {
    vtsls = {},
  },

  setup = function(capabilities)
    -- Setup handled by mason-lspconfig
  end,
}
